import React, { PropTypes } from 'react'
import {H1} from 'native-base';

class App extends React.Component {
    render () {
        return(
            <H1>Hi from App</H1>
        )
    }
}

export default App;
